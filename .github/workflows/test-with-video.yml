name: Appium Tests with Video (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled until self-hosted runner is configured
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow notice
        run: |
          echo "This workflow is temporarily disabled."
          echo "Set up a self-hosted Windows runner with an Android device to enable USB video runs."
